class Character {
  constructor(name, level, status, strategy) {
    this.name = name;
    this.level = level;
    this.status = status;
    this.strategy = strategy;
  }
}

export default Character;
